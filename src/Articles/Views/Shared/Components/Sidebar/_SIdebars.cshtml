@model Articles.Models.WidgetViewModel

<div id="sidebars">
    @Html.Partial("_Categories", Model.Categories)
    @Html.Partial("_Tags", Model.Tags) 
    @Html.Partial("_LatestPosts", Model.Posts)
</div>

