@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper,
    IList<Articles.Core.Tag> tags)
{
foreach (var tag in tags)
{
        <div class="tags-div">
            @Articles.ActionLinkExtensions.TagLink(htmlHelper, tag)
        </div>
}
}
